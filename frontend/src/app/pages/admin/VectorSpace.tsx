// phase_1 §5.3 — placeholder; the 3D vector space lands in Phase 4.
import { EmptyState } from '../../components/design/EmptyState';

export function VectorSpace() {
  return (
    <div className="flex h-full w-full items-center justify-center">
      <EmptyState title="Vector space" description="The 3D point cloud arrives in Phase 4." />
    </div>
  );
}
