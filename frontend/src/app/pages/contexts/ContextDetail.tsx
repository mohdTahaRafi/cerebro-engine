// phase_1 §5.3 — placeholder; the document explorer lands in Phase 2.
import { EmptyState } from '../../components/design/EmptyState';

export function ContextDetail() {
  return (
    <div className="flex h-full w-full items-center justify-center">
      <EmptyState title="Context detail" description="The document explorer arrives in Phase 2." />
    </div>
  );
}
